---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: krateoplatformops.krateo.io
spec:
  group: krateo.io
  names:
    categories:
    - krateo
    kind: KrateoPlatformOps
    listKind: KrateoPlatformOpsList
    plural: krateoplatformops
    singular: krateoplatformops
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                enum:
                - Orphan
                - Delete
                type: string
              install:
                items:
                  properties:
                    insecureSkipTLSVerify:
                      description: |-
                        SkipCRDs skips installation of CRDs for the release.
                        SkipCRDs bool `json:"skipCRDs,omitempty"`
                        InsecureSkipTLSVerify skips tls certificate checks for the chart download
                      type: boolean
                    name:
                      description: Name of Helm chart, required if ChartSpec.URL not
                        set
                      type: string
                    pullSecretRef:
                      description: PullSecretRef is reference to the secret containing
                        credentials to helm repository
                      properties:
                        key:
                          description: The key to select.
                          type: string
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object.
                          type: string
                      required:
                      - key
                      - name
                      - namespace
                      type: object
                    repository:
                      description: 'Repository: Helm repository URL, required if ChartSpec.URL
                        not set'
                      type: string
                    set:
                      description: Set defines the Helm values
                      items:
                        description: SetVal represents a "set" value override in a
                          Release
                        properties:
                          name:
                            type: string
                          value:
                            type: string
                          valueFrom:
                            description: ValueFromSource represents source of a value
                            properties:
                              configMapKeyRef:
                                description: DataKeySelector defines required spec
                                  to access a key of a configmap or secret
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object.
                                    type: string
                                  optional:
                                    type: boolean
                                required:
                                - name
                                - namespace
                                type: object
                              secretKeyRef:
                                description: DataKeySelector defines required spec
                                  to access a key of a configmap or secret
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object.
                                    type: string
                                  optional:
                                    type: boolean
                                required:
                                - name
                                - namespace
                                type: object
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                    url:
                      description: URL to chart package (typically .tgz), optional
                        and overrides others fields in the spec
                      type: string
                    version:
                      description: Version of Helm chart, late initialized with latest
                        version if not set
                      type: string
                    wait:
                      description: |-
                        Namespace to install the release into.
                        Namespace string `json:"namespace"`
                        SkipCreateNamespace won't create the namespace for the release. This requires the namespace to already exist.
                        SkipCreateNamespace bool `json:"skipCreateNamespace,omitempty"`
                        Wait for the release to become ready.
                      type: boolean
                    waitTimeout:
                      description: |-
                        WaitTimeout is the duration Helm will wait for the release to become
                        ready. Only applies if wait is also set. Defaults to 5m.
                      type: string
                  type: object
                type: array
              serviceType:
                type: string
            type: object
          status:
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              releases:
                items:
                  properties:
                    releaseDescription:
                      type: string
                    revision:
                      type: integer
                    state:
                      description: Status is the status of a release
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
